<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/app_background"
    tools:context=".ui.settings.SettingsFragment">

    <!-- Tarjeta de Cache -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cacheCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="@color/white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/cacheStatusTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/settings_cache_title"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Subtitle1"
                android:textColor="@color/gray_dark"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/cacheStatusValue"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Body1"
                android:textColor="@color/gray_medium"
                tools:text="Cached photos: 0"/>

            <TextView
                android:id="@+id/cacheLastUpdate"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
                android:textColor="@color/gray_light"
                tools:text="Last sync: --"/>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Mensaje informativo -->
    <TextView
        android:id="@+id/cacheMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
        android:textColor="@color/gray_dark"
        tools:text="@string/settings_cache_message"/>

    <!-- Botón de limpiar cache -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/clearCacheButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/action_clear_cache"
        app:cornerRadius="12dp"
        app:backgroundTint="@color/bnv_item_checked"
        android:textColor="@color/white"/>

</LinearLayout>
